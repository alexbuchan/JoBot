<div class="profile-page-links">
  <div class="profile-link">
    <a class="link" href="/userProfile">User Profile</a>
    <a class="link" href="/dashboard">Dashboard</a>
    <a class="link" href="/search">Search</a>
  </div>
</div>
<h1>Search Results:</h1>

<div style="overflow: auto; height:400px;">
    <% if(userArray.length===0) { %>
      <ol>
        <% jobs.forEach((job) => { %>
          <div class="job-listing">
            <li>
              TITLE:<%= job.title %> - COMPANY:<%= job.company %> - CITY:<%= job.city %> - REQUIREMENTS:<%= job.requirements %>
              <form action="/job_display/<%= job._id %>" method="POST">
              <button id="applyToJob" class="btn btn-primary active job-apply">APPLY</button>
              </form>
             <%  }); %>
            </li>
          </div>
      </ol>
    <% } else { %>
      <ol>
        <% jobs.forEach((job) => { %>
          <% let newJob = job._id.toString(); %>
          <div class="job-listing">
            <li>
              TITLE:<%= job.title %> - COMPANY:<%= job.company %> - CITY:<%= job.city %> - REQUIREMENTS:<%= job.requirements %>
            <% if(userArray.indexOf(newJob)<0) { %>
              <form action="/job_display/<%= job._id %>" method="POST">
              <button id="applyToJob" class="btn btn-primary active job-apply">APPLY</button>
              </form>
            <% }else { %>
              <form action="/job_display/<%= job._id %>" method="POST">
              <button id="applyToJob" class="btn btn-primary disabled job-applied">APPLIED</button>
               </form>
            <%  } %>
            </li>
          </div>
        <%  }); %>
      </ol>
    <% } %>
  </div>