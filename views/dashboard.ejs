<h1>DashBoard of <%= currentUser.user.username %></h1>

<img src="" alt="User Avatar">

<h2>JOBS APPLIED LIST:</h2>
<p>Number of Jobs: <%= currentUser.user.jobsApplied.length %>		</p>
<p><%= jobs %></p>

   <ol>		
    <% jobs.jobsApplied.forEach((job) => { %>
      <div class="job-listing">
        <li>
          <%= job.title %>
        </li>
      </div>
    <%  }) %>
    </ol>    
	 
<form action="/userProfile">
	<button type="submit" class="btn btn-primary btn-block">User Profile</button>
</form>
<form action="/search">
	<button type="submit" class="btn btn-primary btn-block">Search</button>
</form>
<form action="/logout">
	<button type="submit" class="btn btn-primary btn-block">Logout</button>
</form>

<pre><code>
	<%= user %>
</code></pre>